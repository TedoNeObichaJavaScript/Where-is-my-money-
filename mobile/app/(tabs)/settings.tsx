import { ScreenPlaceholder } from '@/components/layout/ScreenPlaceholder';

/** Settings tab — built out in Phase 10 (tasks 186–198). */
export default function SettingsRoute() {
  return <ScreenPlaceholder title="Settings" />;
}
