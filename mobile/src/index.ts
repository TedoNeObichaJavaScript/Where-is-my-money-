// Source root for Parite mobile.
// Folder map:
//   theme/      design tokens, ThemeProvider, theme primitives
//   domain/     pure models: Money, entities, enums
//   db/         SQLCipher connection, migrations, schema
//   data/       repositories (CRUD + aggregate queries) over db/
//   storage/    MMKV prefs + secure-store key handling
//   stores/     Zustand UI/state stores
//   components/ reusable component kit (refined-dark)
//   i18n/       i18next setup + EN/BG resource bundles
export {};
